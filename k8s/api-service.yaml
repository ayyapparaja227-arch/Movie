apiVersion: v1
kind: Service
metadata:
  name: trendpredict-api-service
  namespace: trendpredict
  labels:
    app: trendpredict-api
spec:
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: trendpredict-api
  type: ClusterIP
